{
  "name": "wc-serial-number",
  "title": "WC Serial Number",
  "description": "The Best WooCommerce Plugin ever made!",
  "version": "1.2.7",
  "homepage": "https://www.pluginever.com",
  "author": {
    "name": "pluginever",
    "email": "support@pluginever.com",
    "url": "https://www.pluginever.com"
  },
  "engines": {
    "node": ">=8.9.3",
    "npm": ">=5.5.1"
  },
  "scripts": {
    "start": "composer install --ignore-platform-reqs && npm install && npm run lint && npm run watch",
    "watch": "byteever-scripts start",
    "build": "byteever-scripts build",
    "postbuild": "npm run i18n",
    "i18n": "grunt i18n",
    "format": "byteever-scripts format-js",
    "lint": "npm run lint:js && npm run lint:css && npm run && npm run lint:php && npm run lint:pkg-json && npm run lint:md",
    "lint:js": "byteever-scripts lint-js  --fix",
    "lint:css": "byteever-scripts lint-style --fix",
    "lint:php": "./vendor/bin/phpcs --standard=phpcs.xml $(git ls-files | grep .php$)",
    "lint:php-fix": "./vendor/bin/phpcbf --standard=phpcs.xml $(git ls-files | grep .php$)",
    "lint:pkg-json": "byteever-scripts lint-pkg-json",
    "lint:md": "byteever-scripts lint-md-docs",
    "analyze-bundles": "byteever-scripts build --webpack-bundle-analyzer",
    "check-engines": "byteever-scripts check-engines",
    "check-licenses": "byteever-scripts check-licenses",
    "packages-update": "byteever-scripts packages-update",
    "test:e2e": "byteever-scripts test-e2e",
    "test:unit": "byteever-scripts test-unit-js"
  },
  "devDependencies": {
    "@byteever/scripts": "git+ssh://git@github.com:byteever/scripts.git",
    "grunt-checktextdomain": "^1.0.1",
    "grunt-wp-i18n": "^1.0.3",
    "grunt-wp-readme-to-markdown": "^2.0.1",
    "husky": "4.3.0",
    "matchdep": "^2.0.0"
  },
  "@byteever/scripts": {
    "devURL": "https://buildwc.test"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.php": [
      "php -d display_errors=1 -l",
      "./vendor/bin/phpcs --extensions=php --warning-severity=8 -s"
    ],
    "*.scss": [
      "stylelint --syntax=scss --fix"
    ],
    "*.{js,jsx}": [
      "wp-scripts format-js",
      "wp-scripts lint-js"
    ]
  }
}
