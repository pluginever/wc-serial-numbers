/**
 * WordPress dependencies
 */
import { render } from '@wordpress/element';

/**
 * Internal dependencies
 */
import App from './App';

render( <App />, document.getElementById( 'wcsn-upgrade-notice' ) );
